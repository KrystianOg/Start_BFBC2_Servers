<UserControl x:Class="Start_BFBC2_Servers.XAML.SettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Start_BFBC2_Servers.XAML"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Folder Path: " Foreground="White" VerticalAlignment="Center"/>
        <TextBox x:Name="PathBlock" Width="100" Grid.Row="0" Grid.Column="1" Text="C:/" Background="{x:Null}" Foreground="Orange" HorizontalAlignment="Left" VerticalAlignment="Center" />

        <Button Grid.Column="1" Grid.Row="1" Height="32" Width="32" Click="Save" Cursor="Hand">
            <Image Source="/Images/save.png" Stretch="Uniform"/>
        </Button>
    </Grid>
</UserControl>
